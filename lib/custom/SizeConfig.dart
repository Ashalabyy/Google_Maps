import 'package:flutter/material.dart';

// The Screen Height
double ScreenHeight(BuildContext context) {
  double ScreenH = MediaQuery.of(context).size.height;

  return ScreenH;
}

// the ScreenHeight of the Big Container the holds the Categories...
double CategorieViewHeight(BuildContext context) {
  double ScreenH = MediaQuery.of(context).size.height;

  return ScreenH / 2.88;
}

// the ScreenHeight of the Container of the Categories Big Container...
double CategorieViewContainerHeight(BuildContext context) {
  double ScreenH = MediaQuery.of(context).size.height;

  return ScreenH / 4.33;
}

// the ScreenHeight of the Stack of the Categories...
double CategorieViewStackHeight(BuildContext context) {
  double ScreenH = MediaQuery.of(context).size.height;

  return ScreenH / 7.21;
}

// FontSize Height 16
double FontSize16(BuildContext context) {
  double ScreenH = MediaQuery.of(context).size.height;

  return ScreenH / 54.1;
}

// Height 20
double Height20(BuildContext context) {
  double ScreenH = MediaQuery.of(context).size.height;

  return ScreenH / 43.3;
}

double Height180(BuildContext context) {
  double ScreenH = MediaQuery.of(context).size.height;

  return ScreenH / 4.8;
}

// Height 10
double Height10(BuildContext context) {
  double ScreenH = MediaQuery.of(context).size.height;

  return ScreenH / 86.6;
}

// Height 15
double Height15(BuildContext context) {
  double ScreenH = MediaQuery.of(context).size.height;

  return ScreenH / 57.5;
}

double Height140(BuildContext context) {
  double ScreenH = MediaQuery.of(context).size.height;

  return ScreenH / 6.18;
}

// FontSize Height 14
double FontSize14(BuildContext context) {
  double ScreenH = MediaQuery.of(context).size.height;

  return ScreenH / 61.7;
}

// The Screen Width
double ScreenWidth(BuildContext context) {
  double ScreenW = MediaQuery.of(context).size.width;

  return ScreenW;
}

// width 210
double Width210(BuildContext context) {
  double ScreenW = MediaQuery.of(context).size.width;

  return ScreenW / 1.95;
}

double Width180(BuildContext context) {
  double ScreenW = MediaQuery.of(context).size.width;

  return ScreenW / 2.28;
}

double Width140(BuildContext context) {
  double ScreenW = MediaQuery.of(context).size.width;

  return ScreenW / 2.93;
}

double Width10(BuildContext context) {
  double ScreenW = MediaQuery.of(context).size.width;

  return ScreenW / 41.1;
}

// width 200
double Width200(BuildContext context) {
  double ScreenW = MediaQuery.of(context).size.width;

  return ScreenW / 2.0;
}
